import React from 'react';
import AuthForm from "../components/UI/Forms/auth/AuthForm";

const Auth = () => {
    return (
        <div className={"section__authentication"}>
            <AuthForm/>
        </div>
    );
};

export default Auth;